import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.indiapost.dop.circulars',
  appName: 'DOP Circulars and Orders',
  webDir: 'build'
};

export default config;
